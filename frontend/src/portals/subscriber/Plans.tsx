/**
 * Plans catalogue page (Story 3.4).
 *
 * STUB — wired into /subscriber/plans by the shared App.tsx route table so the
 * build stays green. The Story 3.4 implementation replaces this placeholder with
 * the real catalogue (usePlans + PlanCard + filter/sort + Current Plan badge).
 */
function Plans() {
  return (
    <main className="max-w-3xl mx-auto px-4 py-8">
      <h1 className="text-2xl font-bold text-neutral-900">Plans</h1>
      <p className="mt-2 text-sm text-neutral-500">Loading plans...</p>
    </main>
  );
}

export { Plans };
