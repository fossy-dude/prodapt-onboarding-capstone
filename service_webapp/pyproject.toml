[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "service-webapp"
version = "0.1.0"
description = "Primary Webservice_backend Service. API endpoints for all frontend API calls (REST and WS calls for agentic workflows)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "psycopg[async,pool]>=3.2",
    "valkey[asyncio]>=6",
    "aiokafka>=0.11",
    "pymilvus[milvus-lite]>=2.4",
    "copilotkit>=0.1",
    "pydantic-settings>=2.3",
    "langgraph>=0.2",
    "langchain-openai>=0.2",
    "langfuse>=2",
    "opentelemetry-sdk>=1.25",
    "opentelemetry-instrumentation-fastapi>=0.46b0",
    "weasyprint>=62",
    "scikit-learn>=1.5",
    "uuid7>=0.1",
]

[project.optional-dependencies]
dev = [
    "ruff>=0.4",
    "pyrefly",
    "pytest>=8",
    "pytest-asyncio>=0.23",
    "httpx>=0.27",
    "testcontainers[postgres,redis,kafka]>=4",
    "tox>=4",
    "tox-uv>=1",
]

# ┌──────────────────────────────────────────────────────────────────────────────────────────────────┐
# │                                           Ruff Config                                            │
# └──────────────────────────────────────────────────────────────────────────────────────────────────┘
# Adopted verbatim from docs/Ref-Linting-config-pyproject.toml (Story 1.3 canonical lint template).
# `src` added so ruff treats service_webapp/src as a first-party import root (layout requirement).

[tool.ruff]
line-length = 120
target-version = "py311"
fix = true
src = ["src"]

[tool.ruff.format]
# 5. Use single quotes for non-triple-quoted strings.
quote-style = "double"
docstring-code-format = true

[tool.ruff.lint]
select = [
    "B", # flake8-bugbear
    "C4", # comprehensions
    "D", # pydocstyle
    "E", # pycodestyle
    "F", # pyflakes
    "FA", # flake8-future-annotations
    "FLY", # flynt
    "G", # flake8-logging-format
    "I", # isort
    "INT", # flake8-gettext
    "ISC", # implicit string concatenation
    "NPY002", # numpy-legacy-random
    "PERF", # Perflint
    "PGH", # pygrep-hooks
    "PIE", # misc lints
    "PL", # pylint
    "PT", # flake8-pytest-style
    "PYI", # flake8-pyi
    "Q", # flake8-quotes
    "RUF", # Ruff-specific rules
    "S102", # flake8-bandit: exec-builtin
    "T10", # flake8-debugger
    "TCH", # type-checking imports
    "TID", # tidy imports
    "UP", # pyupgrade
    "W", # pycodestyle
    "YTT", # flake8-2020
]

# Taken from pandas pyproject.toml as reference
ignore = [
    ### Intentionally disabled
    # module level import not at top of file
    "E402",
    # do not assign a lambda expression, use a def
    "E731",
    # controversial
    "B007",
    # controversial
    "B008",
    # setattr is used to side-step mypy
    "B009",
    # getattr is used to side-step mypy
    "B010",
    # tests use comparisons but not their returned value
    "B015",
    # Function definition does not bind loop variable
    "B023",
    # Only works with python >=3.10
    "B905",
    # Too many arguments to function call
    "PLR0913",
    # Too many returns
    "PLR0911",
    # Too many branches
    "PLR0912",
    # Too many statements
    "PLR0915",
    # Redefined loop name
    "PLW2901",
    # Global statements are discouraged
    "PLW0603",
    # Use `typing.NamedTuple` instead of `collections.namedtuple`
    "PYI024",
    # No builtin `eval()` allowed
    "S307",
    # while int | float can be shortened to float, the former is more explicit
    "PYI041",
    # incorrect-dict-iterator, flags valid Series.items usage
    "PERF102",
    # try-except-in-loop, becomes useless in Python 3.11
    "PERF203",
    # pytest-parametrize-names-wrong-type
    "PT006",
    # pytest-parametrize-values-wrong-type
    "PT007",
    # pytest-patch-with-lambda
    "PT008",
    # pytest-raises-with-multiple-statements
    "PT012",
    # pytest-assert-in-except
    "PT017",
    # pytest-composite-assertion
    "PT018",
    # pytest-fixture-param-without-value
    "PT019",
    # The following rules may cause conflicts when used with the formatter:
    "ISC001",
    ### TODO: Enable gradually
    # Useless statement
    "B018",
    # Magic number
    "PLR2004",
    # comparison-with-itself
    "PLR0124",
    # collection-literal-concatenation
    "RUF005",
    # pairwise-over-zipped (>=PY310 only)
    "RUF007",
    # mutable-class-default
    "RUF012",
    # Unnecessary pass/... literal
    "PIE790",
    # Unused imports
    "F401",
    "E501", # (line too long)
    "G004", # (logging-format-interpolation - use f-strings
    "W291", # Trailing Whitespace
    "W293", # Blank lines contains Trailing Whitespace
    # Docstring styles - too much
    "D100", # Missing docstring in public module
    "D104", # Missing docstring in public package
]
unfixable = []

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["E402"]
"**/{tests,docs,tools}/*" = ["E402"]
"error_codes.py" = ["C408"]
# Tests legitimately omit module/function docstrings; narrow ignore keeps the rule set intact elsewhere.
# PLC0415 (import-not-at-top) is allowed in tests so app/config imports can be deferred
# inside fixtures/tests — this controls when the eager ``settings = Settings()`` singleton
# runs relative to pytest-env populating the required env vars (Story 1.4).
"tests/*" = ["D100", "D101", "D102", "D103", "D104", "D105", "PLC0415"]


[tool.ruff.lint.flake8-pytest-style]
fixture-parentheses = false
mark-parentheses = false

[tool.ruff.lint.flake8-quotes]
docstring-quotes = "double"

[tool.ruff.lint.isort]
case-sensitive = true
combine-as-imports = true
force-wrap-aliases = true
split-on-trailing-comma = true

[tool.ruff.lint.pyupgrade]
# Preserve types, even if a file imports `from __future__ import annotations`.
keep-runtime-typing = true

[tool.ruff.lint.pydocstyle]
# Use numpy-style docstrings.
convention = "numpy"

[tool.ruff.lint.pyflakes]
#A list of modules to ignore when considering unused imports. These will not be removed even if unused
allowed-unused-imports = []


# ┌──────────────────────────────────────────────────────────────────────────────────────────────────┐
# │                                          Pytest config                                           │
# └──────────────────────────────────────────────────────────────────────────────────────────────────┘

[tool.pytest.ini_options]
pythonpath = ["src"]
python_files = "test_*.py"
markers = [
    # Slow running tests
    "slow",
    # Unit, integration and functional tests
    "unit",
    "functional",
    "integration",
    "security",
    # Internal API calls involved. Can be skipped if required
    "api__internal",
]
addopts = [
    "--import-mode=importlib",
    "--strict-config",
    "--strict-markers",
    "--asyncio-mode=auto",
]
# addopts = "--cov --cov-report html --cov-report term-missing"

# pytest-env: provide the REQUIRED env vars so the eager ``settings = Settings()``
# singleton (core/config.py) succeeds at test collection time. Optional secrets
# stay at their defaults. Individual tests override these via monkeypatch.
# (Story 1.4 — config fail-fast must not break collection.)
env = [
    "DB__HOST=localhost",
    "DB__PORT=5432",
    "DB__NAME=sboai_test",
    "DB__USER=sboai_app",
    "DB__PASSWORD=change_me_app",
    "VALKEY_URL=redis://localhost:6379",
    "KAFKA_BROKERS=localhost:9092",
    "LANGFUSE_ENABLED=false",
]
# /run/secrets is a Podman-only mount; absent in dev/test. Silence pydantic-settings'
# informational warning there (the secrets_dir itself is correct for production).
filterwarnings = [
    "ignore:directory \"/run/secrets\" does not exist:UserWarning",
]

# ┌──────────────────────────────────────────────────────────────────────────────────────────────────┐
# │                                          Pyrefly config                                          │
# └──────────────────────────────────────────────────────────────────────────────────────────────────┘

[tool.pyrefly]
project-includes = ["src/*"]
project-excludes = [
    "**/node_modules",
    "**/__pycache__",
    "**/*venv/**/*",
]
[tool.pyrefly.errors]
no-matching-overload = false

# ┌──────────────────────────────────────────────────────────────────────────────────────────────────┐
# │                                               tox                                                │
# └──────────────────────────────────────────────────────────────────────────────────────────────────┘
# Adapted from docs/Ref-Linting-config-pyproject.toml. Uses uv-venv-runner with per-env `deps` instead
# of the template's uv-venv-lock-runner + `uv sync --extra test`, because service_webapp's runtime deps
# (e.g. weasyprint) require system libraries that `uv sync` would try (and fail) to build in CI/dev
# without them. Per-env deps install only the tooling each gate needs. Env names `lint`/`test` match the
# justfile (`uv tox -e lint`/`-e test`) and the CI gate. See Story 1.3 Completion Notes.

[tool.tox]
requires = ["tox>=4.15"]
env_list = ["lint", "test"]

[tool.tox.env_run_base]
runner = "uv-venv-runner"
description = "Run quality gate under {base_python}"
# Do not build/install the project package itself — the lint/type/test tools run
# against source files directly, and src/ is placed on sys.path via [tool.pytest].
package = "skip"

[tool.tox.env.lint]
description = "Run ruff (lint + format check) and pyrefly type checking"
# Story 1.4: the app now imports fastapi/pydantic-settings/opentelemetry/psycopg/valkey,
# so pyrefly needs them resolvable in the lint venv. Only the libs the type-checked
# source imports are listed — the heavy runtime deps (weasyprint, scikit-learn, etc.)
# are intentionally NOT pulled in (they need system libs to build). psycopg uses the
# binary wheel so no libpq-dev is required.
deps = [
    "ruff>=0.12.9",
    "pyrefly>=0.31.0",
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "pydantic-settings>=2.3",
    "opentelemetry-sdk>=1.25",
    "opentelemetry-instrumentation-fastapi>=0.46b0",
    "psycopg[binary,async,pool]>=3.2",
    "valkey[asyncio]>=6",
    "langfuse>=2",
]
commands = [
    ["ruff", "check", "."],
    ["ruff", "format", "--check", "."],
    ["pyrefly", "check"],
]

[tool.tox.env.test]
description = "Run pytest (skip slow tests)"
# Story 1.4: test stack now exercises the FastAPI app + psycopg/valkey adapters +
# testcontainers (Postgres/Valkey) for the /ready integration test. psycopg uses
# the binary wheel (no libpq-dev needed). Heavy runtime deps (weasyprint etc.) are
# excluded for the same reason as the lint env.
deps = [
    "pytest>=8",
    "pytest-env>=1.1.0",
    "pytest-asyncio>=0.23",
    "httpx>=0.27",
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "pydantic-settings>=2.3",
    "opentelemetry-sdk>=1.25",
    "opentelemetry-instrumentation-fastapi>=0.46b0",
    "psycopg[binary,async,pool]>=3.2",
    "valkey[asyncio]>=6",
    "testcontainers[postgres,redis]>=4",
    "langfuse>=2",
]
commands = [["pytest", "-m", "not slow", "{posargs}"]]
